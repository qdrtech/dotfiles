# Minimal Hyprland entrypoint for this dotfiles repo.
# - Theme colors are generated by scripts/theme-switch.sh -> ~/.config/hypr/theme.conf
# - Add your normal Hyprland config after the theme include.

source = ~/.config/hypr/theme.conf

# Add the rest of your Hyprland config here (keybindings, monitors, windows, etc).
