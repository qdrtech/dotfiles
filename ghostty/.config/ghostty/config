#
#
#   _____ _               _   _         
#  / ____| |             | | | |        
# | |  __| |__   ___  ___| |_| |_ _   _ 
# | | |_ | '_ \ / _ \/ __| __| __| | | |
# | |__| | | | | (_) \__ \ |_| |_| |_| |
#  \_____|_| |_|\___/|___/\__|\__|\__, |
#                                  __/ |
#                                 |___/ 
#

# Configuration file for a graphical user interface (GUI) theme and appearance settings.

# Load the colour palette generated by scripts/theme-switch.sh.
# The path is relative to this file, so it resolves to the theme.conf sitting
# next to it. The leading '?' makes the file optional: on a fresh clone, before
# the switcher has ever run, Ghostty starts normally instead of erroring out.
# An included file is applied after the file that pulls it in, so the generated
# palette always wins over anything set here.
config-file = ?theme.conf

# Define the font family and size for the interface.
# '.SF NS Mono' is the macOS system monospaced font.
# The font size is set to 14 pixels.
font-family = .SF NS Mono
font-size = 14

# Enable font features for better rendering
font-feature = +calt
font-feature = +liga

# Set the horizontal padding for the window.
# The values represent the left and right padding in pixels.
window-padding-x = 10,10

# Configure the background blur and opacity for transparent windows.
# 'background-blur-radius' sets the blur intensity to 20 pixels.
# 'background-opacity' sets the window background to 20% opaque (0.2).
background-blur-radius = 20
background-opacity = 0.2

# Enable GTK title bar for the window.
# This setting ensures that the window uses the GTK-style title bar.
gtk-titlebar = true

shell-integration-features = sudo
